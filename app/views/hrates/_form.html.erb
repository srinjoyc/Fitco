<%= form_for(@hrate) do |f| %>
  <% if @hrate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hrate.errors.count, "error") %> prohibited this hrate from being saved:</h2>

      <ul>
      <% @hrate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
